name: "Neolink Fixed"
description: "Neolink RTSP for Reolink cameras - fixed build from source"
version: "0.7.3"
slug: "neolink-fixed"
arch:
  - aarch64
  - amd64
host_network: true
map:
  - type: addon_config
    read_only: False
options:
  mode: rtsp
  log: info
schema:
  mode: list(rtsp|mqtt|dual)
  log: list(info|warn|error|debug)
